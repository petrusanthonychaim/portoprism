const chartData = [["code", "total", "transaction date"]];

      for (let x = 0; x < 10; x++) {
            let a = 'a'
            let b = 1
            let c = 1
             a += 'a'
             b += 2
             c += 3
            chartData.push ([a,b,c])
        }

console.log(chartData);
